<?xml version="1.0" encoding="UTF-8"?>
<!--

     Copyright 2005-2014 Red Hat, Inc.

     Red Hat licenses this file to you under the Apache License, version
     2.0 (the "License"); you may not use this file except in compliance
     with the License.  You may obtain a copy of the License at

        http://www.apache.org/licenses/LICENSE-2.0

     Unless required by applicable law or agreed to in writing, software
     distributed under the License is distributed on an "AS IS" BASIS,
     WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or
     implied.  See the License for the specific language governing
     permissions and limitations under the License.

-->

<!-- ====================================================================== -->
<!--                                                                        -->
<!--  JBoss, the OpenSource J2EE webOS                                      -->
<!--                                                                        -->
<!--  Distributable under LGPL license.                                     -->
<!--  See terms of license at http://www.gnu.org.                           -->
<!--                                                                        -->
<!-- ====================================================================== -->

<project default="build">

  <!-- ================================================================== -->
  <!-- Setup                                                              -->
  <!-- ================================================================== -->
  <property name="etc.dir" value="${basedir}/etc" />
  <property name="output.dir" value="${basedir}/target" />
  <property name="resources.dir" value="${basedir}/src/main/resources" />
  <property name="wildfly.patch.dir" value="${output.dir}/wildfly-patch" />
  <property name="deploy.artifacts.dir" value="${output.dir}/deploy-artifacts" />
  <property name="deploy.artifacts.lib" value="${deploy.artifacts.dir}/lib" />
  <property name="deploy.artifacts.resources" value="${deploy.artifacts.dir}/resources" />

  <!-- ================================================================== -->
  <!-- Initialization                                                     -->
  <!-- ================================================================== -->
  <target name="init">
  </target>

  <!-- ================================================================== -->
  <!-- Distribution                                                       -->
  <!-- ================================================================== -->
  <target name="build" depends="init">

  	<!-- Filter fabric resources -->
    <copy todir="${wildfly.patch.dir}" overwrite="true">
        <fileset dir="${etc.dir}/wildfly" />
		<filterset>
	        <filter token="project.version" value="${project.version}" />
	        <filter token="version.apache.camel" value="${version.apache.camel}" />
	        <filter token="version.apache.felix.configadmin" value="${version.apache.felix.configadmin}" />
	        <filter token="version.apache.felix.http" value="${version.apache.felix.http}" />
	        <filter token="version.apache.felix.log" value="${version.apache.felix.log}" />
	        <filter token="version.apache.felix.metatype" value="${version.apache.felix.metatype}" />
		    <filter token="version.apache.felix.scr" value="${version.apache.felix.scr}" />
	        <filter token="version.apache.spring" value="${version.apache.spring}" />
	        <filter token="version.hawtio" value="${version.hawtio}" />
	        <filter token="version.jboss.gravia" value="${version.jboss.gravia}" />
	        <filter token="version.osgi" value="${version.osgi}" />
      	</filterset>
    </copy>
  	
    <!-- Camel jars -->
    <copy todir="${wildfly.patch.dir}/modules/org/apache/camel/core/main">
    	<fileset dir="${deploy.artifacts.lib}" >
    		<include name="camel-core-${version.apache.camel}.jar" />
        </fileset>
    </copy>
    <copy todir="${wildfly.patch.dir}/modules/org/apache/camel/core/xml/main">
    	<fileset dir="${deploy.artifacts.lib}" >
    		<include name="camel-core-xml-${version.apache.camel}.jar" />
        </fileset>
    </copy>
    <copy todir="${wildfly.patch.dir}/modules/org/apache/camel/spring/main">
    	<fileset dir="${deploy.artifacts.lib}" >
    		<include name="camel-spring-${version.apache.camel}.jar" />
        </fileset>
    </copy>
    
    <!-- Camel Component jars -->
    <copy todir="${wildfly.patch.dir}/modules/org/apache/camel/component/cxf/main">
    	<fileset dir="${deploy.artifacts.lib}" >
    		<include name="camel-cxf-transport-${version.apache.camel}.jar" />
    		<include name="camel-cxf-${version.apache.camel}.jar" />
        </fileset>
    </copy>
    <copy todir="${wildfly.patch.dir}/modules/org/apache/camel/component/jms/main">
    	<fileset dir="${deploy.artifacts.lib}" >
    		<include name="camel-jms-${version.apache.camel}.jar" />
        </fileset>
    </copy>
    <copy todir="${wildfly.patch.dir}/modules/org/apache/camel/component/jmx/main">
    	<fileset dir="${deploy.artifacts.lib}" >
    		<include name="camel-jmx-${version.apache.camel}.jar" />
        </fileset>
    </copy>
  	
    <!-- Gravia jars -->
    <copy todir="${wildfly.patch.dir}/modules/org/jboss/gravia/main">
    	<fileset dir="${deploy.artifacts.lib}" >
			<include name="gravia-provision-${version.jboss.gravia}.jar" />
			<include name="gravia-repository-${version.jboss.gravia}.jar" />
			<include name="gravia-resolver-${version.jboss.gravia}.jar" />
			<include name="gravia-resource-${version.jboss.gravia}.jar" />
			<include name="gravia-runtime-api-${version.jboss.gravia}.jar" />
			<include name="gravia-runtime-embedded-${version.jboss.gravia}.jar" />
			<include name="org.apache.felix.configadmin-*.jar" />
			<include name="org.apache.felix.http.bridge-*.jar" />
			<include name="org.apache.felix.log-*.jar" />
			<include name="org.apache.felix.metatype-*.jar" />
			<include name="org.apache.felix.scr-*.jar" />
        </fileset>
    </copy>
    
    <!-- OSGi jars -->
    <copy todir="${wildfly.patch.dir}/modules/org/osgi/enterprise/main">
    	<fileset dir="${deploy.artifacts.lib}" >
			<include name="org.osgi.enterprise-${version.osgi}.jar" />
        </fileset>
    </copy>
    
    <!-- Springsource jars -->
    <copy todir="${wildfly.patch.dir}/modules/org/springframework/aop/main">
    	<fileset dir="${deploy.artifacts.lib}" >
    		<include name="spring-aop-${version.apache.spring}.jar" />
        </fileset>
    </copy>
    <copy todir="${wildfly.patch.dir}/modules/org/springframework/asm/main">
    	<fileset dir="${deploy.artifacts.lib}" >
    		<include name="spring-asm-${version.apache.spring}.jar" />
        </fileset>
    </copy>
    <copy todir="${wildfly.patch.dir}/modules/org/springframework/beans/main">
    	<fileset dir="${deploy.artifacts.lib}" >
    		<include name="spring-beans-${version.apache.spring}.jar" />
        </fileset>
    </copy>
    <copy todir="${wildfly.patch.dir}/modules/org/springframework/context/main">
    	<fileset dir="${deploy.artifacts.lib}" >
    		<include name="spring-context-${version.apache.spring}.jar" />
        </fileset>
    </copy>
    <copy todir="${wildfly.patch.dir}/modules/org/springframework/core/main">
    	<fileset dir="${deploy.artifacts.lib}" >
    		<include name="spring-core-${version.apache.spring}.jar" />
        </fileset>
    </copy>
    <copy todir="${wildfly.patch.dir}/modules/org/springframework/expression/main">
    	<fileset dir="${deploy.artifacts.lib}" >
    		<include name="spring-expression-${version.apache.spring}.jar" />
        </fileset>
    </copy>
    <copy todir="${wildfly.patch.dir}/modules/org/springframework/jms/main">
    	<fileset dir="${deploy.artifacts.lib}" >
    		<include name="spring-jms-${version.apache.spring}.jar" />
        </fileset>
    </copy>
    <copy todir="${wildfly.patch.dir}/modules/org/springframework/transaction/main">
    	<fileset dir="${deploy.artifacts.lib}" >
    		<include name="spring-tx-${version.apache.spring}.jar" />
        </fileset>
    </copy>
    
    <!-- Wildfly-Camel jars -->
    <copy todir="${wildfly.patch.dir}/modules/org/wildfly/camel/main">
    	<fileset dir="${deploy.artifacts.lib}" >
    		<include name="wildfly-camel-subsystem-${project.version}.jar" />
    		<include name="gravia-container-wildfly-extension-${version.jboss.gravia}.jar" />
        </fileset>
    </copy>
	
    <!-- Hawt.io -->
    <copy tofile="${wildfly.patch.dir}/standalone/deployments/hawtio.war" file="${deploy.artifacts.lib}/hawtio-default-${version.hawtio}.war"/>
    
  	<!-- Zip the wildfly patch -->
  	<zip destfile="${output.dir}/${project.artifactId}-${project.version}.zip" basedir="${wildfly.patch.dir}"/>
  	<tar destfile="${output.dir}/${project.artifactId}-${project.version}.tar.gz" basedir="${wildfly.patch.dir}" compression="gzip"/>
  	
  </target>
</project>
